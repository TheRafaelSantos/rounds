services:
  lotofacil-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lotofacil-web
    command: ["python", "main.py", "--serve", "--host", "0.0.0.0", "--port", "8765"]
    environment:
      TZ: America/Sao_Paulo
      PYTHONPATH: /app/src
    ports:
      - "8765:8765"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped

  lotofacil-calibrator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lotofacil-calibrator
    command:
      [
        "python",
        "main.py",
        "--calibration-lab",
        "--lab-from-concurso",
        "2500",
        "--lab-top-games",
        "5000",
        "--draw-hour",
        "20",
        "--draw-minute",
        "0"
      ]
    environment:
      TZ: America/Sao_Paulo
      PYTHONPATH: /app/src
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
